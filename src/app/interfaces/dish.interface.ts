export interface IDish {
  name: string;
  description: string;
  price: number;
  menuId: number;
}

export interface IDishes {
  id: number;
  name: string;
  description: string;
  price: number;
  type: string;
  menuId: number;
}